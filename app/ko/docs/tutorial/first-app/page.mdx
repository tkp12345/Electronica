import { Callout, Cards, Steps, Tabs } from 'nextra/components'
import { PageHero } from '../../../../components/PageHero'
import { FirstAppIllustration } from '../../../../components/illustrations'

<PageHero
  illustration={<FirstAppIllustration />}
  title="튜토리얼 2편 — 첫 앱 만들기"
  subtitle="빈 폴더에서 Hello World 창이 뜨는 데까지 5분."
  badge="Tutorial · 2/6"
/>

## 폴더 만들기

<Steps>

### 새 프로젝트 폴더
```bash filename="terminal"
mkdir my-electron-app && cd my-electron-app
npm init -y
```

`npm init -y` 는 _"질문 없이 기본값으로 package.json 만들어라"_ 라는 뜻이에요.

### Electron 설치

<Tabs items={['npm', 'pnpm', 'yarn']}>
  <Tabs.Tab>
    ```bash filename="terminal"
    npm install --save-dev electron
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash filename="terminal"
    pnpm add -D electron
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash filename="terminal"
    yarn add -D electron
    ```
  </Tabs.Tab>
</Tabs>

<Callout type="info" emoji="🧒">
`--save-dev` 는 _"이 패키지는 개발할 때만 필요하지, 배포된 앱에는 안 들어가도 돼"_ 표시예요. Electron 자체는 빌드 도구라 dev 분류가 자연스럽습니다.
</Callout>

### `main.js` 만들기 — 앱의 시작점

```js filename="main.js"
const { app, BrowserWindow } = require('electron')

function createWindow() {
  const win = new BrowserWindow({ width: 800, height: 600 })
  win.loadFile('index.html')
}

// 모든 창과 모든 모듈이 준비된 순간
app.whenReady().then(createWindow)

// 맥에서는 dock 아이콘 클릭으로 다시 창 열기
app.on('activate', () => {
  if (BrowserWindow.getAllWindows().length === 0) createWindow()
})

// 윈도우/리눅스: 마지막 창이 닫히면 앱 종료
app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') app.quit()
})
```

<Callout type="info" emoji="🧒">
"앱 준비됨 → 창 생성" 한 줄이 본체고, 나머지 두 개는 _맥/윈도우의 창 닫기 관습 차이를 맞춰주는 보너스_ 입니다.
</Callout>

### `index.html` — 실제로 보일 내용

```html filename="index.html"
<!DOCTYPE html>
<html>
  <body>
    <h1>Hello, Electron 👋</h1>
  </body>
</html>
```

### `package.json` 수정 — 진입점과 실행 스크립트

```json filename="package.json"
{
  "name": "my-electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  }
}
```

### 실행

```bash filename="terminal"
npm start
```

작은 창이 열리고 "Hello, Electron 👋" 가 보이면 성공!

</Steps>

<Callout type="warning">
창이 안 뜨면: ① `electron .` 가 설치 폴더 안에서 실행됐는지, ② `main.js` 가 package.json 의 `main` 과 같은 이름인지 확인하세요.
</Callout>

<Cards>
  <Cards.Card title="다음: Preload 스크립트" href="/ko/docs/tutorial/preload" arrow />
</Cards>
